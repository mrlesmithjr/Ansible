#!/usr/bin/env ruby

procs = `ps aux`
running = false
procs.each_line do |proc|
  running = true if proc.include?('rabbitmq')
end
if running
  puts 'OK - Rabbitmq daemon is running'
  exit 0
else
  puts 'WARNING - Rabbitmq daemon is NOT running'
  exit 1
end
