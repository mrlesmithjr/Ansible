{{ ansible_managed|comment }}
filter {
  if "_grokparsefailure" not in [tags] {
    if "vCenter" in [tags] and "multiline" not in [tags] {
      mutate {
        replace => [ "message", "%{syslog_message}" ]
      }
    }
  }
}
