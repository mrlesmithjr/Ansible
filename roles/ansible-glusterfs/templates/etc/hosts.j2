127.0.0.1  localhost
127.0.1.1  {{ ansible_hostname }}

# The following lines are desirable for IPv6 capable hosts
::1     localhost ip6-localhost ip6-loopback
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters
{% for item in groups[glusterfs_nodes_group] %}
{%   if item != inventory_hostname %}
{{ hostvars[item].ansible_ssh_host }} {{ item }} {% if pri_domain_name is defined %}{{ item }}.{{ pri_domain_name }}{% endif %}

{%   endif %}
{% endfor %}
